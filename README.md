# Mini Project – “Task Manager with Plugins” 
## Goal: show advanced Java, SOLID, patterns.
### Features:
    * Users can create tasks with name, tag, due date, priority, and note.
    * Filter, sort, search tasks.
 - Plugin system:
    * Defines an interface TaskExporter (Strategy pattern).
    * Provide built-in exporters: to CSV, to Markdown.
